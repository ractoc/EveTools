[Service]
WorkingDirectory=/opt/eve
ExecStart=/opt/eve/user.jar
Restart=always
StandardOutput=syslog
StandardError=syslog
SyslogIdentifier=user-app
User=eve
Group=eve
Environment=NODE_ENV=production
[Install]
WantedBy=multi-user.target
